generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model TaxSlab {
  id        Int   @id @default(autoincrement())
  minIncome Float
  maxIncome Float
  rate      Float
}