generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model TaxSlab {
  id        Int   @id @default(autoincrement())
  minIncome Float
  maxIncome Float
  rate      Float
}